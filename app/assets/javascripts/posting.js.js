// Generated by CoffeeScript 1.6.3
var WDI;

WDI = WDI || {};

WDI.app = angular.module("Poster", ["ngResource", 'WDI.factories']);

this.PostingCtrl = [
  "$scope", "Post", "$http", function($scope, Post, $http) {
    $scope.posts = Post.query();
    $scope.addPost = function() {
      var post;
      post = Post.save($scope.newPost);
      console.log(post);
      $scope.posts.push(post);
      return $scope.newPost = {};
    };
    return $scope.addComment = function(post_id, text) {
      console.log(post_id);
      return $http.post("/posts/" + post_id + "/comments", text).success(function() {
        console.log("success!");
        return $scope.posts = Post.query();
      });
    };
  }
];

this.CommentingCtrl = ["$scope", "Comment", "$http", function($scope, Comment, $http) {}];
